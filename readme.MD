🔴 IMPORTANT - READ CAREFULLY! 🔴

> ⚠️ This is an important assignment that must be completed entirely.
> 
> 📋 ***RESOURCES ALLOWED:***
> - ***Your laptop only***
> - ***GitHub Copilot - INLINE CHAT MODE ONLY***
>   -  ❌ NO full chat conversations, NO separate windows
>   -  ❌ NO external resources, documents, or AI chat tools
---

# Starting from the provided template, implement a program that follows the specifications below:

![Sample image](docs/sample.jpg)

- Create the classes described in the diagram,elements inside and represent relationships between them. (3p)

- Extend the program to not allow duplicate employees based on the Employee email. (1p)

- Create a method that can add Employees .Use the predefined Set from EmployeeManager to store and manage employees.
  Call it from the main method and add three employees. (1p)

- Create a method to Display all employee names sorted alphabetically with their salary. (1p)

- Implement updateSpendingById(Long id, Integer salary) that updates the employee’s
  salary by id. (1p)

- Implement a method findByEmail(String email) in EmployeeManager that returns the employee with the given email.
  The search must be case-insensitive and ignore leading/trailing spaces.(1p)

- Write unit tests to verify:
  Employee with given email are correctly found by findByEmail method.(1p)


  #####################################

- Create the classes described in the diagram,elements inside and represent relationships between them. (3p)

- Extend the program to not allow duplicate customers based on the Customer email. (1p)

- Create a method that can add Customers.Use the predefined Set from CustomerManager to store and manage customers.
  Call it from the main method and add three customers. (1p)

- Create a method to Display all customer names sorted alphabetically with their totalSpending. (1p)

- Implement a method findByEmail(String email) in CustomerManager that returns the customer with the given email.
  The search must be case-insensitive and ignore leading/trailing spaces. (1p)

- Implement updateSpendingByEmail(Long id, int amount) that increases the customer’s
  total spending identified by id . (1p)

- Write unit tests to verify:
  Customers with given email are correctly found by findByEmail method.(1p)
